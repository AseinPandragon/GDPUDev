# CodeBuddy 定时任务：游戏开发技术「学习观察简报」每日生成（合规版）

> 本文档是 **CodeBuddy 定时任务**的完整执行说明，由 CodeBuddy 按计划自动触发并执行。任务产出必须始终符合本站定位声明：
>
> **站点定位**：本网站为个人非经营性站点，用于展示个人数字媒体，附带个人习作 Demo 原型下载；同时发布个人对游戏、数字技术行业的学习观察与评述，文章附带参考来源外部链接。仅个人学习展示使用，不从事新闻采编、新闻转载、游戏出版、交易、论坛等服务。

---

## 一、任务定位与合规红线（最高优先级，冲突时以本节为准）

1. **内容定性**：每日产出定性为「个人对游戏、数字技术行业的学习观察与评述」（学习笔记 / 个人简报性质），**不是新闻栏目**。全文不得出现"本站讯""记者""独家消息""据报道本站获悉"等新闻媒体用语。
2. **写作口吻**：所有条目以个人视角撰写，如"笔者观察到""值得学习者关注""从开发角度看"。摘要与正文必须用**自己的话概括 + 个人观察/评述**，禁止整段照搬任何原文。
3. **参考来源**：每条内容必须附带**参考来源外部链接**，且**优先引用一手官方来源**（引擎官方博客/官方公告/官方文档、GitHub 项目主页、开发者个人技术博客、厂商开发者大会页面）。不转载其他媒体的新闻稿全文，不聚合搬运二次报道；二手信息仅可作为线索，落点链接须回溯到一手来源，找不到一手来源则放弃该条。
4. **非经营性**：不含任何广告、付费推广、带货/联盟链接、交易引导、募资引导；不发布可下载的盗版资源或破解工具；不提供论坛/社区功能。
5. **内容边界**：仅聚焦游戏开发技术与行业学习观察（引擎、图形渲染、生态资源、开源项目、实战教程、AI 工具、行业动向）。**不涉及时政、政治人物与敏感议题**；不发布煽动性、攻击性内容；对厂商/个人的评价保持客观克制，以技术学习视角评述。
6. **宁缺毋滥**：搜索不到足够数量符合上述要求的内容时，宁可少于目标条数，也不编造、不降低合规标准。

## 二、任务目标

每天生成【今日】游戏开发技术学习观察简报，覆盖主流引擎与开发技术（Unity、团结引擎、虚幻引擎 UE、Godot 等），包括：

- 引擎版本与官方动态（以官方一手公告为参考来源）
- 图形渲染技术（URP/HDRP/DOTS 等）
- 生态与资源
- GitHub 开源项目精选
- 实战教程（架构 / 性能优化 / C#）
- AI 工具与智能体
- 行业大厂动向 / 投融资 / 展会展讯（以官方公告或一手报道为参考来源，以个人学习观察视角转述）

产出直接写入当日数据文件，并同步更新站点数据源文件，完成后自动 git commit 并 push 到远程仓库。

**执行者**：CodeBuddy 定时任务（每日自动触发，读取本文档并严格执行）
**工作目录**：`e:\Programs\GDPUDev`（站点仓库根目录，即 index.html 所在目录）
**远程仓库**：`origin = https://AseinPandragon@github.com/AseinPandragon/GDPUDev.git`（分支 main，Cloudflare Pages 自动部署）

## 三、数据文件输出规则

1. 生成完整数据并直接写入 `data/当天日期.js`（命名如 `data/2026-09-01.js`，当天日期 = 任务执行当天实际日期 YYYY-MM-DD，星期几对应实际日期）。
2. 若当天文件不存在则新建；若已存在则先读取确认，当天数据已存在时不重复覆盖历史（可跳过并在结果中说明）。

## 四、站点数据源同步（必须执行，不依赖守护程序）

主网页 `index.html` 依赖两个数据源文件：`archive_list.js`（日期下拉索引）与 `news_data.js`（默认展示内容）。每天生成数据后必须显式更新这两个文件。

1. **更新 `archive_list.js`**：在 `window.NEWS_ARCHIVE_LIST` 数组顶部插入一行
   `{date: "当天日期", weekday: "星期 X", file: "data/当天日期.js", desc: "今日最新情报"}`
   同时把原本顶部那条条目的 desc 从 `"今日最新情报"` 改为 `"往期技术归档"`。保留所有历史条目，不得删除。
2. **更新 `news_data.js`**：将当日完整数据（与 `data/当天日期.js` 内容完全一致的 `window.DAILY_NEWS_DATA`）覆盖写入。
3. **编码与换行**：`archive_list.js` 与 `news_data.js` 均为 UTF-8 无 BOM、CRLF 换行，更新时保持该编码与换行；若编辑工具匹配失败，改用 PowerShell 的 `[System.Text.Encoding]::UTF8` 读写文件。

## 五、数据格式规范（严格遵循，直接输出可赋值给 `window.DAILY_NEWS_DATA` 的完整 JS 代码）

```js
window.DAILY_NEWS_DATA = {
  meta: {
    date: "YYYY-MM-DD", weekday: "星期 X",
    title: "广药 游戏开发情报中枢・每日技术情报",
    subtitle: "个人学习观察与评述・附参考来源外部链接",
    updateTime: "YYYY-MM-DD HH:mm", editor: "广药技术中枢 & CodeBuddy",
    highlights: ["今日核心要点 1", "今日核心要点 2", "今日核心要点 3"],
    engineStatus: [{ name, type, status, badge, color }...]
  },
  hero: {
    id, category: "engine|industry|games|opensource|tutorials|ai", categoryName, tag,
    title,                      // 个人观察视角标题，非原文标题照搬
    summary,                    // 100~150 字，自己的话概括 + 个人评述
    image,                      // 配图 URL，没有则留空 ""
    source,                     // 参考来源名称（官方博客/项目名等）
    date, url,                  // url = 一手参考来源链接
    readTime, hotScore, tags,
    content: [{ title, type: "list", items: [...] }, { title, type: "text", text: "...包含个人观察" }]
  },
  items: [ /* 30 条，字段见下 */ ]
};
```

**items 每条字段**：
`id`（唯一英文 ID）/ `category`（engine|industry|games|opensource|tutorials|ai）/ `subcategory` / `title` / `summary`（60~100 字，自己的话概括 + 观察视角）/ `source`（参考来源名称）/ `date` / `url`（一手参考来源链接）/ `image`（有合适配图则填，没有则留空 ""）/ `badge` / `badgeType`（engine|hot|ai|business|game|event）/ `readTime` / `hotScore`（80-95）/ `tags` / `content`（至少 2 个区块，含 list 或 text；其中至少 1 个区块体现个人观察/学习点评）。

## 六、内容获取与质量要求

- 使用通用搜索、image_search 等工具获取当天最新内容与配图。
- 条目数 30 条左右，覆盖 Unity / UE / Godot / 团结引擎 / 行业 / 开源 / 教程 / AI 等全部分类，内容丰富多元；不足 30 条时按合规红线第六条处理。
- 每条 title 必须是个人观察视角的概括或评述式标题，禁止逐字照抄原文标题（可在括号内附原文主题关键词辅助检索）。
- content 至少 2 个区块（list 或 text），至少 1 个区块是"笔者观察 / 学习要点"性质的个人点评，而非纯事实罗列。
- hero 为当日最重磅内容，配图优先横版大图。
- **不编造数据**：搜索不到的不列入；版本号、日期、金额等关键事实必须与参考来源一致。

## 六-A、配图硬性要求（每条尽量配图）

1. **逐条配图**：每条 items 尽量都配图，hero 必配横版大图。写入 image 字段前必须验证图片 URL 可访问（HTTP 200 且 Content-Type 为 image/*），验证不过的 URL 不得写入。
2. **图源优先级**：
   - 首选：抓取参考来源页面的 og:image / twitter:image（用 curl 或脚本提取，注意 HTML 实体转义如 `&amp;` → `&`）
   - 游戏类条目：Steam 官方商店头图 `https://cdn.akamai.steamstatic.com/steam/apps/<appId>/header.jpg`（可用 storesearch API `https://store.steampowered.com/api/storesearch/?term=<名称>&cc=us&l=en` 查 appId），可外链、稳定
   - 官方媒体资产（如 `xboxwire.thesourcemediaassets.com`、`static.pearlcdn.com`、`gaming-cdn.com`）也可用
3. **禁止**：使用与该条内容无关的其他文章配图（会造成图文错配）；使用明显带防盗链、验证后会挂的图源；用截图拼接或fake图。
4. **抓不到图时**：该条保持 `image: ""`（纯文字排版，主网页自动回退不破图）。**不得**为了凑图使用 AI 生成"新闻实拍风格"图片；如确需 AI 插画（非实拍、主题装饰性质），必须先询问用户获得确认。
5. 部分站反爬（已知 gadgets360 / itch.io / unrealengine.com 抓不到 og:image），直接换替代官方图源（如 Steam 头图）或留空，不硬耗。

## 七、参考来源链接硬性要求（发布前逐条验证）

1. 每条 `url` 必须是该条内容对应的**具体文章页 / 具体公告页 / 具体文档页 / GitHub 具体 repo 页**。
2. **严禁**以下类型的链接：网站根链接/首页（如 `https://unity.com/`、`https://godotengine.org/`）、栏目或标签聚合页（如 `/feed?categories=...`、`/news?lang=de`）、搜索结果页或带筛选参数的列表页（如 `?releasetype=release-desc#`）、App Store/商店列表页。
3. **发布前逐条验证**：每条 url 用抓取工具或 curl 确认可访问（HTTP 200）且页面内容与该条摘要相符；验证失败或跳转到首页的，换来源或放弃该条。
4. `source` 填参考来源名称（如 "Unity 官方博客""GitHub: xxx"），优先标注"官方一手来源"。
5. 条目 title/summary 写完后自查：这条链接点进去能不能直接看到本条所述内容？不能 = 链接不合格，重找或删除。

## 八、去重硬性要求

1. 生成今日数据前，必须先读取 `data/` 目录下最近 5 天的归档文件（如 `data/2026-09-20.js` ~ `data/2026-09-24.js`），提取所有已有的新闻标题和 url；辅助参考同目录 `dedup_urls.json`。
2. 今日条目中严禁出现与最近 5 天归档重复的内容（以 url 或标题核心内容判断）。
3. 已出现过的条目必须替换为其他未收录的新内容，确保每日有增量、不炒冷饭。
4. `engineStatus` 等固定信息可保留，但 items 列表和 hero 必须是当日新增内容。

## 九、自动 Git Push 步骤（文件写入与同步后必须执行）

1. 在工作目录 `e:\Programs\GDPUDev` 执行 `git add -A`
2. 执行 `git commit -m "auto: sync daily gamedev data for YYYY-MM-DD"`（替换为当天日期）
3. 执行 `git push origin main`
4. 若 `git status` 显示无变更（流水线脚本已自动提交），跳过 commit 直接 push；push 失败时在结果中说明原因（如网络、凭据过期——凭据使用 `AseinPandragon` 账号）。
5. commit/push 必须包含 `data/当天日期.js`、`archive_list.js`、`news_data.js` 三个文件的变更（仅同步数据源文件时同样提交），确保线上站点自动部署为当天内容。

## 十、完成后告知用户

简要告知：当天学习观察简报已生成并写入 `data/当天日期.js`、已同步 `archive_list.js` 与 `news_data.js`、已去重（与最近 5 天无重复）、全部条目符合个人站点合规定位（学习观察评述 + 一手参考来源链接、无商业内容）、已自动 push 到 GitHub 仓库（AseinPandragon/GDPUDev）并触发线上部署。
